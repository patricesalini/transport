cd ~/transport_temp
python3 -c "
content = '''name: Aircorsica Workflow

on:
  workflow_dispatch:

jobs:
  run-aircorsica:
    runs-on: self-hosted
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Run workflow script
        run: |
          echo 'Workflow started successfully'
'''
with open('.github/workflows/aircorsica.yml', 'w', encoding='utf-8') as f:
    f.write(content)
"
git add .github/workflows/aircorsica.yml
git commit -m "Fix workflow yaml syntax cleanly"
git push origin main

name: Aircorsica Workflow

on:
  workflow_dispatch:

jobs:
  run-aircorsica:
    runs-on: self-hosted
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Run scraper with Xvfb
        run: xvfb-run python scraper_aircorsica.py
